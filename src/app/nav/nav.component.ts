import { Component } from '@angular/core';

@Component({
    selector: 'app-nav',
    templateUrl: './nav.template.html',
    styleUrls: [ './nav.style.scss'],
})

export class Nav {

    /**
     * User information

    public userData;

    constructor(private user: UserService) {
        this.userData = user.getDetails();
    }



    public logout() {
        this.user.logout();
    }
     */
}
